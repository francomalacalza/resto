const db_user = 'root'
const db_password = '1234'
const db_url = 'localhost'
const db_port = '3306'
const db_name = 'resto'

module.exports = {
    user: db_user,
    password: db_password,
    url: db_url,
    port: db_port,
    name: db_name
}